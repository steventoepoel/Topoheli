from pathlib import Path
import re, json
base = Path('/mnt/data/v133')
# index.html rewrite concise manually
index = (base/'client/index.html').read_text()
index = index.replace('<div>\n        <div class="heroBlock"><div class="heroEyebrow">Topo • vlieg • vind</div><h1 class="heroTitle">TopoVlucht <span>Deluxe</span></h1><div class="heroLead">Vlieg supervloeiend naar de juiste plek.</div></div>\n      </div>', '<div class="heroBlock heroHeader"><img class="titleHeli" src="/heli.gif" alt="Helikopter" /><div><div class="heroEyebrow">Topo • vlieg • vind</div><h1 class="heroTitle">TopoVlucht <span>Deluxe</span></h1><div class="heroLead">Vlieg supervloeiend naar de juiste plek.</div></div></div>')
index = index.replace('<div class="startHero">\n          <div>\n            <h2>Kies een opdracht en stijg meteen op</h2>\n          </div>\n          <img class="startHeroHeli" src="/heli.gif" alt="Helikopter" />\n        </div>', '<div class="startHero">\n          <div>\n            <h2>Kies een opdracht en stijg meteen op</h2>\n          </div>\n        </div>')
index = index.replace('<div class="rules">\n          <div class="rule"><span>Tijd</span><strong id="rulesTime">60</strong></div>\n          <div class="rule"><span>Bonus</span><strong>+5s</strong></div>\n        </div>\n        <p class="muted">\n          Bij TopoHeli vlieg je met de helikopter naar de juiste plek. Bij Namen noemen typ je de naam van de plek.\n          Na 3 goede antwoorden krijg je bonus. Landen kunnen oplichten op de wereldkaart.\n        </p>', '<div class="rules">\n          <div class="rule"><span>Tijd</span><strong id="rulesTime">60</strong></div>\n          <div class="rule"><span>Bonus</span><strong>+5s</strong></div>\n        </div>')
index = index.replace('          <div class="field">\n            <label for="assignmentKindInput">Soort plek</label>\n            <select id="assignmentKindInput">\n              <option value="">Automatisch kiezen</option>\n              <option value="city">Steden</option>\n              <option value="capital">Hoofdsteden</option>\n              <option value="country">Landen</option>\n            </select>\n          </div>\n', '')
index = index.replace('<h3>Highscores</h3>\n          <ul id="teacherHighscoreList" class="scoreList"></ul>\n\n          <h3>Meest gemiste plekken</h3>\n          <ul id="teacherMissList" class="scoreList"></ul>', '<h3>Highscores per kaart</h3>\n          <div id="teacherHighscoreList" class="teacherStats"></div>\n\n          <h3>Meest gemiste plekken per kaart</h3>\n          <div id="teacherMissList" class="teacherStats"></div>\n\n          <section id="adminPanel" class="adminPanel hidden">\n            <h3>Docenten beheren</h3>\n            <div class="field">\n              <label for="adminTeacherUsernameInput">Nieuwe docent</label>\n              <input id="adminTeacherUsernameInput" placeholder="gebruikersnaam" />\n            </div>\n            <div class="field">\n              <label for="adminTeacherPasswordInput">Wachtwoord</label>\n              <input id="adminTeacherPasswordInput" type="password" placeholder="wachtwoord" />\n            </div>\n            <div class="actions">\n              <button id="adminCreateTeacherBtn">Docent toevoegen</button>\n            </div>\n            <ul id="adminTeacherList" class="scoreList"></ul>\n          </section>')
index = index.replace('<h2>Docent login</h2>', '<h2>Docent / beheerder login</h2>')
(base/'client/index.html').write_text(index)

css = (base/'client/styles.css').read_text()
css += """
.heroHeader{display:flex;align-items:center;gap:16px}
.titleHeli{width:88px;height:88px;object-fit:contain;filter:drop-shadow(0 10px 14px rgba(0,0,0,.14))}
.startHero{padding:12px 16px;margin-bottom:12px;min-height:auto}
.startHero h2{font-size:1.25rem;margin:2px 0}
.assignmentItem label{padding:10px 12px;border-radius:14px}
.assignmentItem strong{font-size:.98rem}
.assignmentItem .muted{font-size:.82rem}
.teacherStats{display:grid;gap:12px}
.statsCard{padding:12px;border:1px solid var(--line);border-radius:16px;background:#fff}
.statsCard h4{margin:0 0 8px}
.statsCard ul{margin:0;padding-left:18px}
.flashCountry{filter:drop-shadow(0 0 14px rgba(81,214,116,.75))}
.adminPanel{margin-top:16px;padding-top:8px;border-top:1px solid var(--line)}
@media (max-width: 960px){.heroHeader{align-items:flex-start}.titleHeli{width:64px;height:64px}}
"""
(base/'client/styles.css').write_text(css)

app = (base/'client/app.js').read_text()
# state additions
app = app.replace('  pendingHighscore: null\n};', '  pendingHighscore: null,\n  user: null,\n  gameDuration: 60,\n  endsAtMs: 0,\n  hitFlashUntil: 0,\n  hitFlashFeature: null,\n  missionLockedUntil: 0\n};')
# element list
app = app.replace('  "assignmentNameInput","assignmentRegionInput","assignmentDifficultyInput","assignmentKindInput","assignmentPayloadInput","createAssignmentBtn",\n  "fullscreenBtn","gameFullscreenBtn","periodAllBtn","periodWeekBtn","periodDayBtn",\n  "startInlineBtn","summaryScreen","summaryStats","summaryHighscore","summaryNameWrap","summaryNameInput","summarySaveBtn","summaryMenuBtn"',
'  "assignmentNameInput","assignmentRegionInput","assignmentDifficultyInput","assignmentPayloadInput","createAssignmentBtn",\n  "fullscreenBtn","gameFullscreenBtn","periodAllBtn","periodWeekBtn","periodDayBtn",\n  "startInlineBtn","summaryScreen","summaryStats","summaryHighscore","summaryNameWrap","summaryNameInput","summarySaveBtn","summaryMenuBtn",\n  "adminPanel","adminTeacherUsernameInput","adminTeacherPasswordInput","adminCreateTeacherBtn","adminTeacherList"')
# render assignments without badge
app = re.sub(r'div.innerHTML = `\n      <input[^`]+?<div class="badge">\$\{a\.payload\?\.items\?\.length \|\| 0\}</div>\n      </label>`;', 'div.innerHTML = `\n      <input type="radio" id="ass-${a.id}" name="assignmentChoice" ${a.id === state.selectedAssignmentId ? "checked" : ""}>\n      <label for="ass-${a.id}">\n        <div>\n          <div><strong>${a.name}</strong></div>\n          <div class="muted">${a.region || ""}${a.difficulty ? " · " + a.difficulty : ""}</div>\n        </div>\n      </label>`;', app, flags=re.S)
# updateHud timer based on end time
app = app.replace('function updateHud(){\n  els.timeStat.textContent = state.practice ? "∞" : `${Math.ceil(state.timer)}s`;\n  els.scoreStat.textContent = state.score;\n  els.streakStat.textContent = state.streak;\n}', 'function updateHud(){\n  if (!state.practice && state.endsAtMs) {\n    state.timer = Math.max(0, (state.endsAtMs - Date.now()) / 1000);\n    state.elapsedSeconds = state.gameDuration - state.timer;\n  }\n  els.timeStat.textContent = state.practice ? "∞" : `${Math.ceil(state.timer)}s`;\n  els.scoreStat.textContent = state.score;\n  els.streakStat.textContent = state.streak;\n}')
# province overlay add country border
app = app.replace('  state.provinceLayer = L.geoJSON(state.nlProvinceGeo, {\n    interactive:false,\n    style: {\n      color:"#143a63",\n      weight:4.2,\n      opacity:1,\n      fill:false\n    }\n  }).addTo(state.map);\n}', '  state.provinceLayer = L.geoJSON(state.nlProvinceGeo, {\n    interactive:false,\n    style: {\n      color:"#143a63",\n      weight:4.8,\n      opacity:1,\n      fill:false\n    }\n  }).addTo(state.map);\n  const nlFeature = getCountryFeatureByName("Nederland");\n  if (nlFeature) {\n    L.geoJSON(nlFeature, {\n      interactive:false,\n      style:{ color:"#082744", weight:5.5, opacity:1, fill:false }\n    }).addTo(state.provinceLayer);\n  }\n}')
# highlightCountryFeature glow class and keep temp
app = app.replace('function clearHighlightCountry(){\n  if (state.highlightLayer) {\n    state.map.removeLayer(state.highlightLayer);\n    state.highlightLayer = null;\n  }\n}\nfunction highlightCountryFeature(feature){\n  clearHighlightCountry();\n  if (!feature) return;\n  state.highlightLayer = L.geoJSON(feature, {\n    interactive:false,\n    style: {\n      color:"#20a34a",\n      weight:3,\n      fillColor:"#57d879",\n      fillOpacity:0.35\n    }\n  }).addTo(state.map);\n}', 'function clearHighlightCountry(){\n  if (state.highlightLayer) {\n    state.map.removeLayer(state.highlightLayer);\n    state.highlightLayer = null;\n  }\n}\nfunction highlightCountryFeature(feature, sticky=false){\n  clearHighlightCountry();\n  if (!feature) return;\n  state.highlightLayer = L.geoJSON(feature, {\n    interactive:false,\n    className:"flashCountry",\n    style: {\n      color:"#20a34a",\n      weight:4.2,\n      fillColor:"#57d879",\n      fillOpacity:0.42\n    }\n  }).addTo(state.map);\n  if (sticky) {\n    state.hitFlashFeature = feature;\n    state.hitFlashUntil = performance.now() + 1000;\n  }\n}')
# nextMission lock reset
app = app.replace('  state.targetCountryFeature = state.currentAssignment.payload.kind === "country" ? getCountryFeatureByName(state.currentMission.name) : null;\n  clearHighlightCountry();', '  state.targetCountryFeature = state.currentAssignment.payload.kind === "country" ? getCountryFeatureByName(state.currentMission.name) : null;\n  state.missionLockedUntil = 0;\n  if (performance.now() > state.hitFlashUntil) clearHighlightCountry();')
# helper for delayed next naming + complete mission
insert_point = app.find('async function logMiss(){')
helper = '''\nfunction completeMission(feedbackText=""){\n  if (!state.currentMission) return;\n  reward();\n  if (state.currentAssignment?.payload?.kind === "country" && state.targetCountryFeature) {\n    showTarget([state.currentMission.lat, state.currentMission.lng]);\n    highlightCountryFeature(state.targetCountryFeature, true);\n  }\n  if (state.gameMode === "naming") {\n    els.answerFeedback.textContent = feedbackText || `Goed! ${state.currentMission.name}`;\n    state.missionLockedUntil = performance.now() + 1100;\n    setTimeout(() => {\n      if (!state.gameActive) return;\n      nextMission();\n    }, 1100);\n  } else {\n    nextMission();\n  }\n}\n\n'''
app = app[:insert_point] + helper + app[insert_point:]
# finishGame totalSeconds based on Date.now if not practice? fine current state.elapsedSeconds.
# loop dt and timer logic, world detection etc
app = app.replace('function loop(ts){\n  if (!state.gameActive) return;\n  const dt = Math.min(0.025, ((ts - state.lastTs)/1000) || 0.016);\n  state.lastTs = ts;', 'function loop(ts){\n  if (!state.gameActive) return;\n  const rawDt = ((ts - state.lastTs)/1000) || 0.016;\n  const dt = Math.min(0.03, rawDt);\n  state.lastTs = ts;')
app = app.replace('    const boost = state.keys.space ? 1.08 : 1;\n    const maxSpeed = 420 * boost;', '    const boost = state.keys.space && state.currentAssignment?.region === "Wereld" ? 1.22 : 1;\n    const maxSpeed = 390 * boost;')
app = app.replace('    if (state.currentAssignment.payload.kind === "country") {\n      const overTargetCountry = pointInGeometry(center.lat, center.lng, state.targetCountryFeature?.geometry);\n      if (overTargetCountry) {\n        showTarget([state.currentMission.lat, state.currentMission.lng]);\n        highlightCountryFeature(state.targetCountryFeature);\n      } else {\n        hideTarget();\n        clearHighlightCountry();\n      }\n      if (overTargetCountry) {\n        reward();\n        nextMission();\n      }\n    } else {\n      if (dist <= near) showTarget([state.currentMission.lat, state.currentMission.lng]); else hideTarget();\n      if (dist <= hit){\n        reward();\n        nextMission();\n      }\n    }\n    if (performance.now() - state.missionStartedAt >= 10000) els.nextMissionBtn.classList.remove("hidden");\n  }\n\n  if (!state.practice){\n    state.timer -= dt;\n    state.elapsedSeconds = (state.gameMode === "naming" ? 90 : 60) - Math.max(state.timer, 0);\n    if (state.timer <= 0){\n      state.timer = 0;\n      state.elapsedSeconds = (state.gameMode === "naming" ? 90 : 60);\n      updateHud();\n      finishGame(false);\n      return;\n    }\n  } else {\n    state.elapsedSeconds = (performance.now() - state.startedAt) / 1000;\n  }', '    if (state.currentAssignment.payload.kind === "country") {\n      const overTargetCountry = pointInGeometry(center.lat, center.lng, state.targetCountryFeature?.geometry);\n      const fallbackHit = dist <= (state.currentAssignment.region === "Wereld" ? 52 : 30);\n      if (overTargetCountry || fallbackHit) {\n        showTarget([state.currentMission.lat, state.currentMission.lng]);\n        highlightCountryFeature(state.targetCountryFeature);\n      } else if (performance.now() > state.hitFlashUntil) {\n        hideTarget();\n        clearHighlightCountry();\n      }\n      if ((overTargetCountry || fallbackHit) && performance.now() >= state.missionLockedUntil) {\n        completeMission();\n      }\n    } else {\n      if (dist <= near) showTarget([state.currentMission.lat, state.currentMission.lng]); else if (performance.now() > state.hitFlashUntil) hideTarget();\n      if (dist <= hit && performance.now() >= state.missionLockedUntil){\n        completeMission();\n      }\n    }\n    if (performance.now() - state.missionStartedAt >= 10000) els.nextMissionBtn.classList.remove("hidden");\n  }\n\n  if (!state.practice){\n    state.timer = Math.max(0, (state.endsAtMs - Date.now()) / 1000);\n    state.elapsedSeconds = state.gameDuration - state.timer;\n    if (state.timer <= 0){\n      state.timer = 0;\n      state.elapsedSeconds = state.gameDuration;\n      updateHud();\n      finishGame(false);\n      return;\n    }\n  } else {\n    state.elapsedSeconds = (Date.now() - state.startedAt) / 1000;\n  }\n  if (state.hitFlashFeature && performance.now() <= state.hitFlashUntil) {\n    showTarget([state.currentMission?.lat ?? state.map.getCenter().lat, state.currentMission?.lng ?? state.map.getCenter().lng]);\n    if (!state.highlightLayer) highlightCountryFeature(state.hitFlashFeature);\n  } else if (state.hitFlashFeature && performance.now() > state.hitFlashUntil) {\n    state.hitFlashFeature = null;\n    if (state.currentAssignment?.payload?.kind !== "country") clearHighlightCountry();\n  }')
# startGame times
app = app.replace('  state.pendingHighscore = null;\n  state.timer = state.gameMode === "naming" ? 90 : 60;\n', '  state.pendingHighscore = null;\n  state.gameDuration = state.gameMode === "naming" ? 90 : 60;\n  state.timer = state.gameDuration;\n')
app = app.replace('  state.startedAt = performance.now();', '  state.startedAt = Date.now();\n  state.endsAtMs = Date.now() + state.gameDuration * 1000;\n  state.hitFlashUntil = 0;\n  state.hitFlashFeature = null;')
# submitAnswer correct delayed
app = app.replace('  if (state.gameMode !== "naming" || !state.currentMission) return;\n  if (correctAnswer(els.answerInput.value, state.currentMission.name)){\n    reward();\n    nextMission();\n  } else {', '  if (state.gameMode !== "naming" || !state.currentMission || performance.now() < state.missionLockedUntil) return;\n  if (correctAnswer(els.answerInput.value, state.currentMission.name)){\n    completeMission(`Goed! ${state.currentMission.name}`);\n  } else {')
# check session uses me response and admin panel
app = app.replace('async function checkSession(){\n  try {\n    await api("/api/auth/me");\n    els.teacherPanel.classList.remove("hidden");\n    els.teacherLogoutBtn.classList.remove("hidden");\n    await loadTeacherAssignments();\n    await loadTeacherHighscores();\n    await loadTeacherMisses();\n  } catch {\n    els.teacherPanel.classList.add("hidden");\n    els.teacherLogoutBtn.classList.add("hidden");\n  }\n}', 'async function checkSession(){\n  try {\n    const me = await api("/api/auth/me");\n    state.user = me.user;\n    els.teacherPanel.classList.remove("hidden");\n    els.teacherLogoutBtn.classList.remove("hidden");\n    els.adminPanel.classList.toggle("hidden", state.user?.role !== "admin");\n    await loadTeacherAssignments();\n    await loadTeacherHighscores();\n    await loadTeacherMisses();\n    if (state.user?.role === "admin") await loadTeachers();\n  } catch {\n    state.user = null;\n    els.teacherPanel.classList.add("hidden");\n    els.teacherLogoutBtn.classList.add("hidden");\n    els.adminPanel.classList.add("hidden");\n  }\n}')
app = app.replace('async function logoutTeacher(){\n  await api("/api/auth/logout", { method:"POST" });\n  els.teacherPanel.classList.add("hidden");\n  els.teacherLogoutBtn.classList.add("hidden");\n}', 'async function logoutTeacher(){\n  await api("/api/auth/logout", { method:"POST" });\n  state.user = null;\n  els.teacherPanel.classList.add("hidden");\n  els.teacherLogoutBtn.classList.add("hidden");\n  els.adminPanel.classList.add("hidden");\n}')
# teacher highscores/misses routes grouped render
app = app.replace('async function loadTeacherHighscores(){\n  const a = currentAssignment();\n  if (!a || els.teacherPanel.classList.contains("hidden")) return;\n  const rows = await api(`/api/highscores?assignmentId=${a.id}&gameMode=${state.gameMode}&period=all`).catch(() => []);\n  els.teacherHighscoreList.innerHTML = rows.length\n    ? rows.map(r => `<li><strong>${r.player_name}</strong> — ${r.score} <button class="deleteMini" data-id="${r.id}">Verwijder</button></li>`).join("")\n    : "<li>Nog geen highscores.</li>";\n  els.teacherHighscoreList.querySelectorAll(".deleteMini").forEach(btn => {\n    btn.addEventListener("click", async () => {\n      if (!confirm("Highscore verwijderen?")) return;\n      await api(`/api/admin/highscores/${btn.dataset.id}`, { method:"DELETE" });\n      await loadTeacherHighscores();\n      await loadHighscores();\n    });\n  });\n}\nasync function loadTeacherMisses(){\n  if (els.teacherPanel.classList.contains("hidden")) return;\n  const rows = await api("/api/admin/stats/missed").catch(() => []);\n  els.teacherMissList.innerHTML = rows.length\n    ? rows.map(r => `<li><strong>${r.place_name}</strong> — ${r.misses} missers</li>`).join("")\n    : "<li>Nog geen statistieken.</li>";\n}', 'async function loadTeacherHighscores(){\n  if (els.teacherPanel.classList.contains("hidden")) return;\n  const groups = await api(`/api/admin/stats/highscores`).catch(() => []);\n  els.teacherHighscoreList.innerHTML = groups.length ? groups.map(g => `\n    <div class="statsCard"><h4>${g.assignmentName}</h4><ul>${g.rows.length ? g.rows.map(r => `<li><strong>${r.player_name}</strong> — ${r.score} <button class="deleteMini" data-id="${r.id}">Verwijder</button></li>`).join("") : "<li>Nog geen highscores.</li>"}</ul></div>\n  `).join("") : "<div class=\"statsCard\">Nog geen highscores.</div>";\n  els.teacherHighscoreList.querySelectorAll(".deleteMini").forEach(btn => {\n    btn.addEventListener("click", async () => {\n      if (!confirm("Highscore verwijderen?")) return;\n      await api(`/api/admin/highscores/${btn.dataset.id}`, { method:"DELETE" });\n      await loadTeacherHighscores();\n      await loadHighscores();\n    });\n  });\n}\nasync function loadTeacherMisses(){\n  if (els.teacherPanel.classList.contains("hidden")) return;\n  const groups = await api("/api/admin/stats/missed").catch(() => []);\n  els.teacherMissList.innerHTML = groups.length ? groups.map(g => `\n    <div class="statsCard"><h4>${g.assignmentName}</h4><ul>${g.rows.length ? g.rows.map(r => `<li><strong>${r.place_name}</strong> — ${r.misses} missers</li>`).join("") : "<li>Nog geen missers.</li>"}</ul></div>\n  `).join("") : "<div class=\"statsCard\">Nog geen statistieken.</div>";\n}')
# createAssignment no kind
app = app.replace('      difficulty: els.assignmentDifficultyInput.value.trim(),\n      preferredKind: els.assignmentKindInput.value || null,\n      placeNames,', '      difficulty: els.assignmentDifficultyInput.value.trim(),\n      placeNames,')
# add admin funcs before toggleFullscreen
ins = app.find('async function toggleFullscreen(){')
admin_funcs = '''async function loadTeachers(){\n  if (state.user?.role !== "admin") return;\n  const rows = await api("/api/admin/users").catch(() => []);\n  els.adminTeacherList.innerHTML = rows.length ? rows.map(r => `<li><strong>${r.username}</strong> — ${r.role}${r.role === "teacher" ? ` <button class="deleteMini" data-id="${r.id}">Verwijder</button>` : ""}</li>`).join("") : "<li>Nog geen docenten.</li>";\n  els.adminTeacherList.querySelectorAll(".deleteMini").forEach(btn => btn.addEventListener("click", async () => {\n    if (!confirm("Docent verwijderen?")) return;\n    await api(`/api/admin/users/${btn.dataset.id}`, { method:"DELETE" });\n    await loadTeachers();\n  }));\n}\nasync function createTeacher(){\n  const username = els.adminTeacherUsernameInput.value.trim();\n  const password = els.adminTeacherPasswordInput.value;\n  if (!username || !password) return alert("Vul gebruikersnaam en wachtwoord in.");\n  await api("/api/admin/users", { method:"POST", body: JSON.stringify({ username, password, role:"teacher" }) });\n  els.adminTeacherUsernameInput.value = "";\n  els.adminTeacherPasswordInput.value = "";\n  await loadTeachers();\n}\n\n'''
app = app[:ins] + admin_funcs + app[ins:]
# bind events for next direct and admin create
app = app.replace('  els.nextMissionBtn?.addEventListener("click", async () => { registerMiss(); await logMiss(); nextMission(); });', '  els.nextMissionBtn?.addEventListener("click", () => { registerMiss(); logMiss(); nextMission(); });')
app = app.replace('  els.summaryMenuBtn?.addEventListener("click", backToMenuAfterSummary);', '  els.summaryMenuBtn?.addEventListener("click", backToMenuAfterSummary);\n  els.adminCreateTeacherBtn?.addEventListener("click", createTeacher);')
# built-in zoom world more in teacher maybe no.
(base/'client/app.js').write_text(app)

# patch server auth + admin routes in existing files
middleware = (base/'server/src/middleware/auth.js').read_text()
middleware += '\nexport function requireAdmin(req, res, next) {\n  requireAuth(req, res, () => {\n    if (req.user?.role !== "admin") return res.status(403).json({ error: "Alleen voor beheerder." });\n    next();\n  });\n}\n'
(base/'server/src/middleware/auth.js').write_text(middleware)

auth = (base/'server/src/routes/authRoutes.js').read_text()
auth = auth.replace('const user = users.find(u => u.username === username);', 'const user = users.find(u => String(u.username).toLowerCase() === String(username).toLowerCase());')
(base/'server/src/routes/authRoutes.js').write_text(auth)

high = (base/'server/src/routes/highscoreRoutes.js').read_text()
# let's inspect existing then overwrite comprehensively maybe easier
